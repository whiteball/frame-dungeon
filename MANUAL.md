# Frame-Dungeon 操作マニュアル

ワイヤフレーム風のダンジョンを一人称視点で探索するゲームです。このマニュアルでは、キーボードとマウスでの操作方法を説明します。

---

## 1. タイトル画面

画面下部のボタンをマウスでクリックして選びます。

| ボタン | 内容 |
| --- | --- |
| ゲーム開始 | 最初から新しいゲームを始めます |
| ロード | 保存済みのデータを読み込んで再開します |
| 設定 | 視界・操作などの各種設定を開きます（後述） |

---

## 2. 移動・視点

ダンジョン内ではマス単位で移動します。向きは東西南北の4方向です。

| キー | 動作（初期設定） | ターン |
| --- | --- | --- |
| W | 前進（向いている方向へ1マス進む） | 消費する |
| S | その場で180°回転（後ろを向く） | 消費しない |
| A | 左へ90°回転 | 消費しない |
| D | 右へ90°回転 | 消費しない |
| Q | 左へカニ歩き（向きを変えず左へ1マス） | 消費する |
| E | 右へカニ歩き（向きを変えず右へ1マス） | 消費する |
| Shift + S | 後ろへカニ歩き（向きを変えず後ろへ1マス） | 消費する |

- **回転はターンを消費しません。** 周囲を見回すだけなら敵は動きません。
- **移動（前進・カニ歩き）はターンを消費します。** 1マス動くたびに敵や状態異常などのターンが進みます。
- 壁や、敵・宝箱などのふさがれたマスへは進めません。
- 一部の状態異常（スタンなど）の間は移動できません。

> A/D と Q/E の役割（回転とカニ歩き）、および S と Shift+S の役割は、**設定で入れ替え**できます（後述）。

---

## 3. 戦闘・調査

| キー | 動作 |
| --- | --- |
| スペース | 正面の敵を攻撃。状況に応じて自動で挙動が変わります（下記参照） |
| Shift + スペース | 方向を選ばずに、攻撃できる敵を自動で攻撃します（下記参照） |
| C | 前方を調べる（調査） |

### スペースキーの挙動

押したときの周囲の状況によって動作が変わります。

- **正面にのみ敵がいる** → そのまま正面の敵を攻撃します。
- **前方の斜め（左前・右前）にも敵がいる** → 「左 / 中央 / 右 / キャンセル」のボタンが表示され、攻撃する方向を選びます。
- **前方に敵が1体もいない** → 正面のマスを調べます（C キーで中央を選んだ場合と同じ動作）。手軽に1ターンを消費して周囲の様子を進めたいときにも使えます。

### Shift + スペースキーの挙動（自動攻撃）

前方斜めにも敵がいる場合に方向選択メニューを出さず、攻撃できる敵を自動で1体攻撃します。攻撃する相手は **正面 → 右前 → 左前** の順で優先されます。素早く攻撃を繰り返したいときに便利です。

- 前方斜めに敵がいない場合は、通常のスペースキーと同じ動作（正面攻撃、または敵がいなければ正面を調査）になります。

### C キー（調査）

「左 / 中央 / 右 / キャンセル」のボタンが表示され、調べたい方向を選びます。隠し扉のレバーや、回復ポイント・祭壇などのイベントマス、宝箱などはこの調査で起動します。

---

## 4. 画面下部のアクションボタン

ゲーム中は画面下部に操作ボタンが並びます。マウスでクリックするか、対応する**数字キーのショートカット**で実行できます。ボタンは左から順に `1`〜`0`（最大10個）に割り当てられます。

初期状態のボタンと数字キーの対応は次の通りです。

| キー | ボタン | 内容 |
| --- | --- | --- |
| 1 | スキル | 習得しているスキルの一覧を開き、使用します |
| 2 | アイテム | 所持アイテムの一覧を開きます（使用・装備・投げる・置くをここから選びます。後述） |
| 3 | ステータス | 現在のレベル・能力値・状態などを表示します |
| 4 | 足下 | 今いるマスに対するアクション（後述） |
| 5 | セーブ | 現在の状況を保存します |

### 「アイテム」ボタンについて

所持しているアイテムの一覧が開きます（消耗品が上に並びます）。**W / S（↑ / ↓）** で項目を選び、画面下部の専用ボタンで操作します。下部ボタンも数字キーに対応します。

| キー | ボタン | 内容 |
| --- | --- | --- |
| 1 | 使用 | 選択中の消耗品を使います |
| 2 | 装備 | 選択中の武器・防具を装備／解除します |
| 3 | 投げる | 選択中のアイテムを投擲します（後述。装備中のものは不可） |
| 4 | 置く | 選択中のアイテムを今いるマスに置きます（装備中のものは不可） |
| 5 | 説明 | 選択中のアイテムの説明をログに表示します |
| 6 | 閉じる | 一覧を閉じます |

- 選択中のアイテムに使えない操作のボタンは押せなくなります。
- **Enter / スペース**は、選択中が消耗品なら「使用」、装備品なら「装備」を実行します（よく使う操作を素早く行えます）。
- **Escape**でも一覧を閉じられます。

### 「投げる」について

「アイテム」一覧で装備していない所持アイテムを選んで「投げる」を選び、続いて 左 / 中央 / 右 の方向を選ぶと、その方向へ直線に投げます。ターンを消費します。

- 壁・扉・障害物に当たると、その手前のマスに落ちます（置けないマスなら近くの空きマスへ。トラップのマスは避けます）。
- 敵に当たると、アイテムは消えて効果を発揮します。
  - 武器 → その武器を装備して攻撃したのと同じダメージ。
  - 薬などの消費アイテム → 状態異常の付与・解除や能力値変化など、敵に適用できる効果のみ（回復薬は敵を回復させてしまうので注意）。
  - 一部の特殊なアイテム → 専用の投擲効果（爆発など）。
  - 上記に当てはまらない防具などは、当たっても効果がありません（投げ損）。
- 射程は設定により決まり、装備やスキルで延ばせる場合があります。麻痺・睡眠などで動けないときは投げられません。

### 「足下」ボタンについて

今立っているマスの状況に応じて動作します。ターンは消費しません。

- 階段の上 → 「進む / やめる」の確認が出ます。
- すでに見つけているトラップの上 → 「起動 / やめる」の確認が出ます。
- イベントマスの上 → そのイベントを起動します。
- 何もない場合 → 所持アイテムをその場に置く操作になります。

---

## 5. ミニマップ操作

画面にはこれまで歩いた範囲を示すミニマップが表示されます。

| 操作 | 動作 |
| --- | --- |
| M | ミニマップの「ズーム表示 / 全体表示」を切り替え |
| ミニマップを左クリック | 上と同じく表示を切り替え |
| Shift + M | （ズーム表示中のみ）スクロールできる「移動モード」に入る／出る |
| ミニマップを右クリック（ズーム表示中） | スクロールできる移動モードに入る |
| ミニマップを右クリック（全体表示中） | クリックした位置を中心に、移動モードに入る |
| Escape | 移動モードから抜ける |

### 移動モード中の操作

ミニマップの移動モードに入ると、地図をスクロールして遠くの様子を確認できます。

- **W / A / S / D** … ミニマップをスクロール（マップの端より外へは出ません）。
- このモード中はプレイヤーの移動や攻撃はできず、画面下部のボタンは「キャンセル」のみになります。
- 「キャンセル」ボタンまたは Escape で元の表示に戻ります。

---

## 6. ダイアログ・確認画面

行動の途中で、選択肢のボタンが表示されることがあります（攻撃方向の選択、階段やトラップの確認、長く同じ階に留まったときの警告、調査方向の選択、各種一覧の表示など）。

これらが表示されている間は、移動・攻撃などの通常操作は受け付けられません。ボタンをマウスでクリックするか、表示されているボタンに対応する数字キーで選んでください。「やめる」「キャンセル」を選べば元の状態に戻ります。

---

## 7. 設定で変更できる操作

タイトル画面の「設定」から、各種設定を変更できます。設定内容は次回以降も保存されます。

### 操作に関わる項目

| 設定項目 | 内容 |
| --- | --- |
| Q/E で左右回転、A/D で左右カニ歩き | オンにすると、A/D と Q/E の役割（回転とカニ歩き）が入れ替わります |
| S 単体で後退カニ歩き（Shift+S で180°回転） | オンにすると、S と Shift+S の役割が入れ替わります |

### 表示・その他の項目

| 設定項目 | 内容 |
| --- | --- |
| プレイヤーの視界 | プレイヤーが見渡せるマスの範囲を数値で設定します。大きいほど遠くまで見えます |
| フォグの有無 | オンにすると、未探索の部分が暗く隠されます。オフにすると隠されません |
| 常に敵を表示 | オンにすると、視界やフォグに関係なく敵が常に表示されます |
| デバッグコマンド | 開発・動作確認用のコマンドを有効にします。通常のプレイでは使いません |

---

## 8. 操作のまとめ（早見表）

| キー / 操作 | 動作 |
| --- | --- |
| W | 前進 |
| S | 180°回転（設定で後退カニ歩きに変更可） |
| A / D | 左 / 右回転（設定でカニ歩きに変更可） |
| Q / E | 左 / 右カニ歩き（設定で回転に変更可） |
| Shift + S | 後退カニ歩き（設定で180°回転に変更可） |
| スペース | 正面の敵を攻撃 / 敵がいなければ正面を調査 |
| Shift + スペース | 方向選択なしで自動攻撃（正面 → 右前 → 左前 の優先順） |
| C | 前方を調べる |
| 1〜0 | 画面下部のアクションボタンを左から順に実行 |
| M | ミニマップ ズーム / 全体 切り替え |
| Shift + M | ミニマップ移動モード 入 / 出 |
| Escape | ミニマップ移動モードから抜ける |
| 左クリック | ボタン選択 / ミニマップ表示切り替え |
| 右クリック | ミニマップ移動モードに入る |
